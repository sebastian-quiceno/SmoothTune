spring:
  profiles:
    active: dev

  application:
    name: smoothtune

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG

storage:
  location: uploads/songs